# CLAUDE.md

## Project Overview

HD Research Hub — open-source AI research platform for Huntington's Disease drug discovery. Personal research project by a data scientist exploring how AI can help accelerate HD research. Educational, not clinical.

**Live site:** https://hd-research-agent.vercel.app
**Repo:** github.com/jravinder/hd-research-agent

## Tone & Philosophy

- **First principles** — question every assumption. If nobody's doing it, that's the reason to start. (Musk)
- **Bold** — push limits. Try things. Ship. (Jensen)
- **Rigorous** — run experiments, not slides. Let data speak. 100 overnight runs > 1 perfect plan. (Karpathy)
- **Educational** — if a beginner can't follow, rewrite it. (Ng)
- **Human-centered** — AI is a tool, not the hero. Ground every claim. When you don't know, say so. (Fei-Fei Li)
- **"The Art of the Possible"** — always frame positively. Lead with progress, breakthroughs, opportunities
- **Never negative** — no doom stats, no "patients dying", no "0 approved treatments"
- **Honest about AI limitations** — say what worked AND what didn't in every experiment
- **Not competing** with HDSA, HDBuzz, HDYO — link to them, complement with AI/data lens
- **We are data scientists, not doctors** — always disclaim, never make clinical claims

## Repository Structure

```
hd-research-agent/
├── index.html                # Main dashboard (auto-generated by build_site.py)
├── experiment-1.html         # Experiment #1 write-up (web version)
├── privacy.html              # Privacy policy
├── terms.html                # Terms of use
├── data/
│   ├── data.json             # Live data from all sources (auto-refreshed daily)
│   ├── papers_full.json      # Expanded paper corpus for experiments
│   ├── experiment_001_report.md   # Experiment #1 full report (markdown)
│   └── experiment_001_results.json # Raw experiment results
├── src/
│   ├── data_fetcher.py       # Pulls from PubMed, ClinicalTrials.gov, HDBuzz, Open Targets
│   ├── build_site.py         # Generates index.html from data.json, deploys via git push
│   ├── literature_agent.py   # PubMed paper fetcher + LLM analyzer
│   ├── repurposing_scanner.py # 16 HD targets, drug hypothesis generator
│   ├── trial_tracker.py      # ClinicalTrials.gov monitor
│   ├── autoresearch.py       # Karpathy-style overnight research loop
│   ├── run_experiment.py     # Full experiment pipeline → report
│   └── llm.py                # Direct Ollama API (NO litellm — removed due to CVE)
└── .github/workflows/
    └── daily-update.yml      # GitHub Actions: daily data refresh at 8am UTC
```

## Development Commands

```bash
# Rebuild site from live data (fetches + generates + deploys)
cd src && python3 build_site.py

# Rebuild without deploying
python3 build_site.py --no-deploy

# Just fetch fresh data
python3 data_fetcher.py

# Run literature agent (PubMed papers + LLM analysis)
python3 literature_agent.py --query "huntington disease treatment" --days=30

# Run drug repurposing scanner
python3 repurposing_scanner.py

# Run full experiment (papers → LLM analysis → hypotheses → report)
python3 run_experiment.py

# Overnight autoresearch loop
python3 autoresearch.py --hours=8 --delay=5

# Check active HD trials
python3 trial_tracker.py
```

## Infrastructure

- **LLM:** Ollama on Jetson AGX Orin (192.168.4.124:11434) or local Mac (localhost:11434)
- **Model:** llama3.1:8b (default), set HD_AGENT_MODEL env var to change
- **Hosting:** Vercel (auto-deploys on git push)
- **CI:** GitHub Actions daily at 8am UTC
- **No litellm** — removed due to TeamPCP supply chain attack (CVE in 1.82.7/1.82.8). Uses direct requests to Ollama /api/chat

## Key Patterns (Learned from Cosmos Cookoff Hackathon)

### Cached results for demo reliability
Like refereai-cosmos cached Cosmos Reason 2 outputs for offline playback, experiments here save raw results to data/experiment_*_results.json. The site can render from cached data even if APIs are down.

### Edge-first inference
All LLM inference runs on Jetson or Mac via Ollama. Zero cloud AI dependencies. Zero cost per run. This is what makes overnight autoresearch loops feasible.

### Vercel + static HTML
Same pattern as refereai-cosmos: static HTML on Vercel, no framework, instant deploys. build_site.py generates the HTML, git push triggers deploy.

### Agentic pipeline pattern
From cosmos_sports_agent.py: input → LLM (with domain prompt) → parse structured output → score → feed back in. The autoresearch loop follows the same pattern: papers → LLM analysis → hypotheses → score → refine → repeat.

### Chain-of-thought for transparency
Like Cosmos Reason 2's <think> tags, experiment reports show the LLM's reasoning — what it found, what it scored high/low, and why. Transparency builds trust.

## What Sets This Apart

Our core differentiator: **chat with the actual research, not summaries.**

Most HD resources show titles, abstracts, or journalist summaries. We pull full-text papers from PubMed Central, chunk them by section, and let people ask questions against the actual Methods, Results, and Discussion sections. The chatbot cites specific paper sections, not just titles.

This compounds over time:
- Every paper scout run adds full-text chunks to the knowledge base
- Every experiment we run adds hypotheses and findings
- The chatbot gets smarter as the corpus grows
- Nobody else is doing this for HD

Protect this feature. Every new agent, experiment, or data source should feed into the knowledge base. The KB is the moat.

## Data Sources (all free, public APIs)

| Source | API | Auth | What we pull |
|--------|-----|------|-------------|
| PubMed | E-utilities | None | HD research papers |
| ClinicalTrials.gov | REST API v2 | None | Active HD trials |
| HDBuzz | RSS feed | None | Community news |
| Open Targets | GraphQL | None | Gene-target associations |

## UI Rules (Sunrise Hope Design System)

- Warm gold/amber palette (#F59E0B, #92400E), coral accent (#FB7185)
- Warm parchment background (#fdf9e9), stone text tones for readability
- 18px body text, Inter font, tight tracking on headlines
- Mobile-first: hamburger nav, responsive grids, horizontal scroll for tables
- Google Translate + Sarvam AI for 22 Indian languages
- Every external link opens in new tab with icon
- Every data point links back to its source
- No em dashes. No anxiety-driven dashboards. No teal.

## Experiment Publishing Guidelines

Follow the Ng (The Batch) / Karpathy (arxiv-sanity) approach:
1. Lead with the question, not the answer
2. Show methodology clearly — reproducible by anyone
3. Be honest about what worked and what didn't
4. Highlight genuinely interesting findings, don't hype
5. Always include: disclaimer, reproducibility steps, source links
6. Rate AI hypotheses honestly — flag when the model got something wrong

## Security

- No litellm (supply chain attack target)
- pip-audit installed, run before deploying
- No API keys in code — Ollama is local, public APIs need no auth
- .env files gitignored
- Privacy policy: we collect nothing, all data from public sources
